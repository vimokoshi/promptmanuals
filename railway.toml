[build]
builder = "NIXPACKS"
builderConfig = { "NIXPACKS_PLAN" = "nodejs_22" }
rootDirectory = ""

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 180
restartTimeout = 60

[start]
command = "node .next/standalone/server.js"
