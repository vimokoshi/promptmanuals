# Prompt Manuals — Session Handover
**Date:** 2026-04-23
**Session Type:** Vercel-to-GitHub backup setup · repo cleanup

---

## Executive Summary

Full backup of `vamatium/promptmanuals.com` Vercel project to private repo `vimokoshi/pmx`. Repo now contains everything needed to restore to Vercel in one command.

**What changed this session:**
1. Vercel CLI re-linked to `getwebsiteco-9773` account (was previously in `vamatium` scope)
2. `vimokoshi/pmx` populated with full source backup (~164MB, 2,700+ files)
3. `.github/workflows/vercel-backup.yml` — GitHub Actions auto-backup every 6h
4. `scripts/restore-vercel.sh` — one-command restore script
5. Main repo: backup workflow + restore script committed (`f78eff76`)

**Nothing deployed to Vercel this session.**

---

## 1 — Vercel-to-GitHub Backup

### pmx Repo (backup target)
**URL:** https://github.com/vimokoshi/pmx
**Visibility:** Private
**Current commits:**
```
e14dd61 full backup promptmanuals.com from Vercel (latest)
57aecba workflow + restore script
73253b9 clean nested subprojects from pmx backup
ea4c4e0 initial backup from Vercel (2851 files)
```

### pmx Repo Contents
```
app/                   — Next.js App Router pages
src/                   — React components, lib, hooks, i18n
packages/              — Shared packages
messages/             — i18n translation files (root-level)
docker/                — Dockerfile, bootstrap.sh, supervisord.conf
scripts/
  restore-vercel.sh    — one-command restore entrypoint
.github/workflows/
  vercel-backup.yml    — GitHub Actions: every 6h + manual trigger
next.config.ts
package.json + package-lock.json
tsconfig.json
wrangler.toml
.vercelignore / .dockerignore
AGENTS.md, CLAUDE.md, CONTRIBUTING.md, CREDENTIALS.md, README.md
```

### pmx Excludes (correctly omitted)
`node_modules/`, `.next/`, `.vercel/`, `app/.next/`, `app/node_modules/`, nested `app/app/`, `app/messages/`, `app/plugins/`, `app/prisma/migrations/`, `*.log`, `.env*`, `plans/`, `docs/`, `.claude-archived/`, `.omc/`, `.windsurf/`

### Vercel Project Details
| Field | Value |
|-------|-------|
| Vercel account | `getwebsiteco-9773` (personal) |
| Team scope | `vamatium` (promptmanuals.com project lives here) |
| Project ID | `prj_dGEO3cfgJWyb2QTgmx0ZiOc9bdPA` |
| Org ID | `team_eynfBSLB3HYyqJsWIG11urnr` |
| Dashboard | https://vercel.com/getwebsiteco/promptmanuals |
| Deploy URL | https://promptmanualscom-vamatium.vercel.app |

### GitHub Access (confirmed)
- Account: `vimokoshi` (logged in via `gh auth`)
- Token scopes: `repo`, `workflow`
- pmx repo permissions: `admin`, `push`, `pull`

---

## 2 — Restore Procedure

### Option A: One-command restore (recommended)
```bash
bash scripts/restore-vercel.sh
# Requires: GH_PAT + VERCEL_TOKEN env vars
```

### Option B: Manual restore
```bash
git clone https://github.com/vimokoshi/pmx.git /tmp/restore
cd /tmp/restore
npm ci
vercel link --org=team_eynfBSLB3HYyqJsWIG11urnr --project=promptmanuals.com --yes
vercel env pull .vercel/.env.production.local --yes
vercel deploy --prod --yes
```

### Option C: Via GitHub Actions (manual trigger)
1. Go to https://github.com/vimokoshi/pmx/actions
2. Click "Backup to GitHub" → "Run workflow" → "Run workflow"

---

## 3 — GitHub Actions Secrets (still needed)

**Add to pmx repo:** https://github.com/vimokoshi/pmx/settings/secrets/actions → New repository secret

| Secret | Where to get it |
|--------|-----------------|
| `VERCEL_TOKEN` | https://vercel.com/account/tokens → Create token |
| `GH_PAT` | https://github.com/settings/tokens → Classic PAT with `repo` scope |

Without these secrets, the workflow won't be able to pull from Vercel or push to pmx.

---

## 4 — Main Repo Changes (local promptmanuals.com)

### New files committed this session
```
f78eff76 chore: add Vercel backup workflow and restore script
         .github/workflows/vercel-backup.yml  (NEW)
         scripts/restore-vercel.sh            (NEW)
```

### New files (not yet committed)
- `.github/workflows/` — workflow exists in main repo ✓
- `scripts/restore-vercel.sh` — exists in main repo ✓

---

## 5 — Current Vercel Deploy Status

| Metric | Value |
|--------|-------|
| Latest deploy | `dpl_EAZkZ3HpvyWbx5f2LERQo4sQjphi` |
| Status | `● Error` (failed deploy, 20h ago) |
| Local git HEAD | `3a7c0727` (backed up to pmx) |
| pmx backup HEAD | `e14dd61` (matches local) |

pmx backup = latest code state. Vercel deploy failed but didn't update production code.

**75% error rate on recent deploys** — backup captures code, not fixing runtime errors.

---

## 6 — Pending from Previous Sessions

Still open from HANDOVER-2026-03-27b:

| Priority | Task | Status |
|----------|------|--------|
| 1 | Reddit OAuth fix (WF08) — needs `client_id` + `client_secret` | No change |
| 2 | Google Docs one-time harvest (~150 prompts) | No change |
| 3 | OpenPrompt CSV import (~5K prompts) | No change |
| 4 | Scheduled Import chunked fetch fix (prompts.chat) | No change |
| 5 | Homepage redesign | No change |
| 6 | Sitemap submit to Google Search Console | No change |
| — | Deploy SEO fixes to Vercel | No change (deploy failed) |

---

## Key URLs & Credentials

| Service | URL |
|---------|-----|
| Production site | https://www.promptmanuals.com |
| Backup repo | https://github.com/vimokoshi/pmx |
| Vercel dashboard | https://vercel.com/getwebsiteco/promptmanuals |
| n8n instance | https://autoxumu.honestwebs.com |
| Supabase | https://supabase.com/dashboard/project/krhxuvuexnlliewhyznm |

All credentials: `CREDENTIALS.md` (gitignored)

---

**End of handover.**
